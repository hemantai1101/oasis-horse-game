# Run 006 — 2026-03-17

**Goal:** Fix feature blindness identified in Run 005 — give the model game-aware features so it can understand the actual win condition.

**What changed vs Run 005:**

| What | Run 005 | Run 006 | Why |
|------|---------|---------|-----|
| Features | 41 (raw positions only) | 105 (game-aware) | Model can't see axis alignment, path clarity, or backstop setup |
| Input size | 41 | 105 | Matches new feature count |
| Architecture | 41→256→128→1 | 105→256→128→1 | Input layer updated, hidden layers unchanged |
| Data | games_50k_4_.5_.15.jsonl | games_50k_4_.5_.15_v2.jsonl | Features change requires full regeneration |

---

## Why Feature Engineering

The win condition requires:
1. A horse aligned on center axis (col=6 or row=6)
2. A clear path from that horse to center (6,6)
3. A friendly backstop piece at (6,5), (6,7), (5,6), or (7,6)

The 41 raw features give the model normalised (col, row) pairs — it cannot infer axis alignment,
path clarity, or backstop occupation from coordinates alone without enormous data and capacity.

---

## New Feature Layout (105 total)

**Per horse × 20 horses = 100 features:**

| Index offset | Feature | Description |
|-------------|---------|-------------|
| +0 | `col_norm` | (col - 1) / 10 — existing |
| +1 | `row_norm` | (row - 1) / 10 — existing |
| +2 | `dist_to_center` | Manhattan distance to (6,6) / 10 — **new** |
| +3 | `on_axis` | 1.0 if col=6 or row=6, else 0.0 — **new** |
| +4 | `path_clear` | 1.0 if on-axis AND no piece between horse and center — **new** |

**Global × 5 features:**

| Index | Feature | Description |
|-------|---------|-------------|
| 100 | `backstop_(6,5)` | +1=current player, -1=opponent, 0=empty — **new** |
| 101 | `backstop_(6,7)` | same — **new** |
| 102 | `backstop_(5,6)` | same — **new** |
| 103 | `backstop_(7,6)` | same — **new** |
| 104 | `player_indicator` | 0.0=P1, 1.0=P2 — existing |

---

## Files to Update

- [x] `training/game.py` → `state_to_features()` — add new feature logic
- [x] `public/ai-worker.js` → `stateToFeatures()` — synced with game.py (Web Worker architecture)
- [x] `training/train.py` → input size `41 → 105`
- [x] `training/README.md` → update feature count and model architecture section
- [x] Regenerate data (depth=4, 50k games)
- [x] Retrain (100 epochs, same hyperparams)

---

## Data Generation

```bash
pypy3 training/generate_games.py \
  --n-games 50000 \
  --workers 14 \
  --depth 4 \
  --time-limit 0.5 \
  --epsilon 0.15 \
  --output training/data/games_50k_4_.5_.15_v2.jsonl
```

Games completed: 49,693 / 50,000 (307 skipped, 0.6%)
Records written: 5,142,012
Generation time: 102,089.2s (~28.4 hrs) — 0.49 games/s
Note: ~3.3h slower than Run 004 due to richer state_to_features (path_clear board traversal)

---

## Training

```bash
python3 training/train.py \
  --data training/data/games_50k_4_.5_.15_v2.jsonl \
  --epochs 100 \
  --batch-size 2048 \
  --lr 1e-3 \
  --weight-decay 1e-4
```

---

## Training Results

Best val loss: **0.62254** (epoch 99)
Train loss at epoch 100: 0.61271
Train/val gap: 0.00983 (no overfitting)
Converged at epoch: **~95** — improvement epoch 95→100 only 0.00024, essentially flat
Training time: 8,573.0s (~2.4 hrs)
Parameters: 60,929

Val loss curve (selected epochs):
| Epoch | Train Loss | Val Loss |
|-------|-----------|----------|
| 1 | 0.70475 | 0.69020 |
| 25 | 0.64074 | 0.64956 |
| 50 | 0.62970 | 0.63743 |
| 75 | 0.61875 | 0.62674 |
| 90 | 0.61391 | 0.62299 |
| 95 | 0.61312 | 0.62278 |
| 100 | 0.61271 | 0.62259 |

Notes:
- ✅ **Biggest single improvement across all runs** — val loss 0.691 → 0.622 (↓0.069 vs Run 005)
- ✅ Feature engineering gave 4× more improvement than tripling model size (Run 004→005 only gained 0.016)
- ✅ Nearly at parity with depth-3 ceiling (0.608) despite depth-4 being a harder prediction problem
- ✅ No overfitting — train/val gap stable at ~0.01 throughout
- ✅ Model converged cleanly at ~epoch 95
- Feature engineering hypothesis confirmed — the model needed vocabulary to express win conditions
- Training took 2.4 hrs vs ~35 min for Run 005 — extra time due to 105 features × 5M records loading
- **Next step: export and test in-game — can it beat the human now?**

---

## Expected Outcome vs Actual

- ✅ "val loss drops below 0.60" — got 0.622, very close, feature engineering confirmed as bottleneck
- Next lever: if in-game still weak → more data (100k games) or deeper search depth (5)

---

## Model

- Saved to: `training/models/model.pt` → back up as `training/models/model_run006.pt`
- Exported to: `public/model/weights.json`
- Tested in browser: *(pending)*
